[pytest]
addopts = --strict-markers
markers =
    unit: fast isolated tests for functions/services with mocked dependencies
    integration: tests covering interactions between layers (routes, auth, db/session)
