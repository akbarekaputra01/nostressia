[pytest]
addopts = --strict-markers
markers =
    unit: fast isolated tests for schema, preprocessing, and helper logic
    integration: tests covering artifact loading/contracts/notebook execution flows
